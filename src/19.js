function randomNodejsCode() {
  // Simulate creating a simple JavaScript object with properties and values
  const obj = { name: "John", age: 30, city: "New York" };
  
  return `const myObject = ${JSON.stringify(obj)};`;
}
